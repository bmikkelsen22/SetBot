import * as React from 'react';
import * as ReactDOM from 'react-dom';
import * as Add from './add/add';

const element = document.getElementById('main-element');
ReactDOM.render(<Add.Component />, element);